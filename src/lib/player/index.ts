// Por ahora, esto es temporal hasta que definamos el proveedor
export const player = {
    getEpisodeUrl(animeId: number, episodeNumber: number): string {
        // Esto es un placeholder - lo reemplazaremos después
        return `https://www.example.com/embed/${animeId}/${episodeNumber}`;
    }
};